function solve(firstNumber, secondNumber) {

    const result = solution(firstNumber, secondNumber);
    console.log(result);

    function solution(firstNumber, secondNumber){

        return (Math.pow(firstNumber, secondNumber));
    }

}

solve(2, 8);